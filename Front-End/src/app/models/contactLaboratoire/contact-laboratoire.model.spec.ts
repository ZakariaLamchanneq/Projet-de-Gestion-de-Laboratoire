import { ContactLaboratoire } from './contact-laboratoire.model';

describe('ContactLaboratoire', () => {
  it('should create an instance', () => {
    expect(new ContactLaboratoire()).toBeTruthy();
  });
});
